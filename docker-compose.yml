version: '2'

services:
  app:
    image: juan321/remesas_peru:latest
    container_name: remesas_peru
    ports:
      - "4200:4200"
    #environment:
    #  MY_ENVIRONMENT: ${MY_ENVIRONMENT}
    #volumes:
    #  - /ruta/en/el/host:/ruta/en/el/contenedor
